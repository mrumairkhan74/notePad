<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-6">

    <h1 class="text-red-400 text-center text-4xl font-bold mb-6">Edit Your Note</h1>

    <form class="w-full max-w-lg" action="/note/edit/<%=notes._id%>" method="post">
        <div>
            <label for="title" class="block text-lg mb-1">Title</label>
            <input 
                type="text" 
                name="title" 
                id="title"
                value="<%= notes.title %>" 
                class="border-white border-2 rounded-md w-full p-3 bg-zinc-800 text-white"
                readonly
            />
        </div>

        <div class="mt-4">
            <label for="content" class="block text-lg mb-1">Your Notes</label>
            <textarea 
                name="content" 
                id="content" 
                class="border-white border-2 w-full p-3 rounded-md bg-zinc-800 text-white resize-none h-40"
            ><%= notes.content %></textarea>
        </div>

        <button 
            type="submit" 
            class="mt-4 bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded w-full"
        >
            Update Note
        </button>
    </form>

</body>
</html>
